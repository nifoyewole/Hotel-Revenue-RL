.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Build artefacts: regenerated by notebooks 01-06. Everything here can be
# rebuilt from data/raw/hotel_bookings.csv, so none of it is tracked.
results/*.joblib
results/*.npy

# The raw dataset is redistributed with the submission zip, not the repository.
data/raw/*.csv
data/processed/*.csv

# Figures and the results table are kept in the repository: they are the
# evidence the report cites, and they are small.
!figures/*.png
!results/*.csv

!**/.gitkeep
